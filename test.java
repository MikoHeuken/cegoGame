public class test {
  public static  void main(String[] args) {
    
    /*cego_game game = new cego_game(0,0);
    cego_player test = new cego_player(100, true, 1);
    test.setCard(0, new cego_card(0));
    test.setCard(1, new cego_card(14));
    test.setCard(2, new cego_card(10));
    test.setCard(3, new cego_card(36));
    test.printCards();

    int[] cards = {new cego_card(30).getNr(), new cego_card(4).getNr(), -1};

    System.out.println(game.isValidCard(cards[0], test, 3));
    System.out.println(cego_card.getNameFromNr(cards[0]));
    System.out.println(test.getCards()[3].getCardName());
    //test.getCards()[game.leastValuePossible(test, erste.getNr())].print();
    //game.turn(test, cards);

    //System.out.println(game.isValidCard(erste.getNr(), test, 1));

    /*for(int i = 0; i < 38; i++){
      cego_card test = new cego_card(i);
      test.print();
    }*/

  }
}
